# Gelistirme: Vite dev sunucusu, hot reload. /api proxy'si vite.config.js'te.
FROM node:22-alpine
WORKDIR /uygulama
COPY package.json package-lock.json* ./
RUN npm install
COPY . .
ENV VITE_API_HEDEF=http://backend:3001
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
